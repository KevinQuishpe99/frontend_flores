server {
    listen 80;
    server_name flowerspaulas.com www.flowerspaulas.com;
    root /usr/share/nginx/html;
    index index.html;

    # Redirigir HTTP a HTTPS (descomentar después de configurar SSL)
    # return 301 https://$server_name$request_uri;

    # Compresión gzip mejorada
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json image/svg+xml application/font-woff application/font-woff2;
    gzip_disable "msie6";

    # Cache para assets estáticos (optimización de descargas)
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot|webp)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # Cache para HTML (más corto)
    location ~* \.html$ {
        expires 1h;
        add_header Cache-Control "public, must-revalidate";
    }

    # SPA - todas las rutas van a index.html
    location / {
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        add_header Pragma "no-cache";
        add_header Expires "0";
    }

    # Headers de seguridad mejorados (prevención de inyecciones)
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://accounts.google.com; style-src 'self' 'unsafe-inline'; img-src 'self' data: https: http:; font-src 'self' data:; connect-src 'self' https://backend-flores-mcsf.onrender.com https://accounts.google.com https://oauth2.googleapis.com; frame-src 'self' https://accounts.google.com;" always;
    add_header Permissions-Policy "geolocation=(), microphone=(), camera=()" always;

    # Prevenir acceso a archivos ocultos
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }

    # Tamaño máximo de archivo
    client_max_body_size 10M;

    # Timeouts
    client_body_timeout 12;
    client_header_timeout 12;
    keepalive_timeout 15;
    send_timeout 10;

    # Ocultar versión de nginx
    server_tokens off;

    # Logs
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log warn;
}

# Configuración HTTPS (descomentar después de configurar SSL con Let's Encrypt)
# server {
#     listen 443 ssl http2;
#     server_name flowerspaulas.com www.flowerspaulas.com;
#     root /usr/share/nginx/html;
#     index index.html;
#
#     # Certificados SSL (Let's Encrypt)
#     ssl_certificate /etc/letsencrypt/live/flowerspaulas.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/flowerspaulas.com/privkey.pem;
#
#     # Configuración SSL moderna
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#
#     # HSTS
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#
#     # Resto de la configuración igual que HTTP
#     # ... (copiar configuración de arriba)
# }

